import { PageLoaderRoute } from "@/components/PageLoaderRoute";

/** Next.js loading UI — no params prop; locale from pathname on client. */
export default function LocaleRouteLoading() {
  return <PageLoaderRoute />;
}
