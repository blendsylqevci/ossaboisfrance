# Scratch scripts

One-off migration, inspection, and repair scripts used during WordPress → Payload migration.

- **Not imported** by the Next.js app at runtime.
- Safe to ignore for production deployments.
- Prefer new tooling under `scripts/` for repeatable ops.

Do not run against production without a backup and explicit review.
